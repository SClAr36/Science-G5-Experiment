# Science G5 workspace rules

## 核心课程教案

当用户要求新建或改写 `核心课程备课/` 中的教案时：

1. 先阅读 `核心课程备课/01-单摆周期.md`，以它作为结构、语言密度和可读性的主要模板。
2. 优先使用清晰的少量一级章节：课程定位与学习目标、重要概念、实验材料、实验过程、板书与记录框架、课堂流程串讲、教师参考/预期结果、安全与备用方案、关键术语。不要把一份教案拆成大量零散的一级章节。
3. 每个实验写清楚用量、步骤、对照、预期结果和教师追问；先写课堂可直接执行的内容，再放教师参考解释。
3a. 课堂主体必须便于授课时快速扫读：优先用短句、动词开头和“讲 / 做 / 问 / 板书 / 记录”等固定标签；答案只保留教师需要说出的关键点。完整材料用量、安全边界和失败备用方案可以保留在相应位置或文末速查，避免在课堂流程中重复长篇解释。
4. 教案中提出的核心问题、教师追问、讨论题和离堂问题都必须紧邻给出教师参考回答或预期答案要点。问答统一采用上下结构：问题单独一行，参考回答直接写在该问题下一段；不得把答案接在问题同一行，也不得先集中列完问题再另列答案。
5. 所有重要概念、课堂笔记、板书标题、记录表术语和关键名词都必须提供准确的中英文双语；首次出现时使用“中文（English）”或紧邻的中英对照，不只在文末词汇表补译。
6. 库存和安全判断必须以 `器材及试剂/试剂识别与保存状态评估.md` 的最新记录为准。
7. 已经上过的实验只调用已有证据和结论，除非用户明确要求，不重复安排操作。
8. 每次生成或更新教案时，必须同时生成同名 `.md` 和 `.html`；两者内容必须来自同一版 Markdown。
9. HTML 沿用 `01-单摆周期.html` 的样式，并加载 `styles/lesson-responsive.css`，确保电脑和 iPad 横竖屏可读。Markdown 完成后运行：

   ```bash
   python3 scripts/render_lesson_html.py \
     '核心课程备课/<教案名>.md' \
     '核心课程备课/<教案名>.html'
   ```

10. HTML 中的“教师核心问答 / 问 / 板书 / 笔记 / 记录”默认渲染为标题始终可见的折叠卡片；“讲 / 做 / 实验 / 知识点”保持展开。折叠卡片应支持电脑点击和 iPad 触控，打印时自动显示完整内容；Markdown 仍保留全部正文。
11. 交付前确认两个文件都存在、HTML 标题正确、主要标题和表格均已渲染，并在 `核心课程备课/README.md` 中维护相应链接与实际授课状态。
